# wand
Touch via USB-HID

## Requirements
- libevdev (Ubuntu package: `libevdev-dev`)
- user rights to access touch event devices (group `input`)

## Build using CMake
- `cmake . -B build`
- `cmake --build build`
